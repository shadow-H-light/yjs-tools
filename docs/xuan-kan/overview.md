# 选刊神器 · 概述

本机上的国际刊筛选工具：按方向匹配，再按分区、指标、审稿时长对比。

只筛选、只匹配，不投稿。默认跑在自己电脑上，不是网站。

## 已拍板

| 编号 | 决策 | 结论 | 备注 |
| --- | --- | --- | --- |
| D1 | 期刊范围 | 先做国际刊 | 中文核心、会议录放到后续或二期 |
| D2 | 产品边界 | 只做筛选匹配；分区/IF 用导入器 | 不负责投稿；不爬商业库当默认同步源 |
| D3 | 技术栈 | 方案 B（本地版） | FastAPI + SQLite + 薄网页；见 [tech-stack.md](./tech-stack.md) |
| D4 | 产品形态 | 本机功能性软件，可做插件 | 不是大体量在线应用；见 [shape.md](./shape.md) |

改决策时：改本表，并在文末「变更记录」加一行。

## 用户能做什么

1. 本机启动后，用刊名、ISSN、缩写查找国际期刊。
2. 看分类、引用、（导入后的）分区与影响因子、（有数据时的）审稿时长。
3. 输入研究方向或关键词，得到按匹配度排序的期刊，并看到命中了哪些主题。
4. 按分区、指标筛选，勾选 2–4 本刊对比。
5. 从本机选择 CSV，导入自己有权使用的分区/IF 表。

二期：在 Zotero 或浏览器里调用同一个本机服务。

## 明确不做（第一期）

- 代投、自动投稿、账号登录出版社。
- 做成需要注册的网站、云同步、多用户后台。
- 默认同步 JCR / 中科院官方全表，或抓取 Clarivate、LetPub、知网。
- 用估算分区冒充官方分区。
- 默认依赖 Docker / PostgreSQL 才能启动。
- 一次做完导师、学校、就业、公司四个工具。
- 第一期就做插件（API 先稳定）。

## 和 yjs-tools 的关系

选刊神器是第一个**本机工具**。后面的模块优先加成同一本地服务里的新命令或新路由，而不是做成一个大应用。

## 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-09 | 初稿。锁定 D1 国际刊、D2 导入器+不投稿。D3 待选。 |
| 2026-09-09 | 锁定 D3 方案 B（本地版）、D4 本机小工具/可插件。 |
| 2026-09-09 | M0/M1 落地：本机同步 OpenAlex，刊名/ISSN 检索可用。 |
